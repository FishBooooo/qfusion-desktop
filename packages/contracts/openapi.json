{
  "components": {
    "schemas": {
      "HealthResponse": {
        "additionalProperties": false,
        "description": "Non-sensitive health metadata returned to the desktop shell.",
        "properties": {
          "api_version": {
            "const": "v1",
            "title": "Api Version",
            "type": "string"
          },
          "data_mode": {
            "const": "synthetic-m0",
            "title": "Data Mode",
            "type": "string"
          },
          "execution_mode": {
            "const": "research",
            "title": "Execution Mode",
            "type": "string"
          },
          "llm_mode": {
            "enum": [
              "off",
              "local",
              "hybrid",
              "cloud"
            ],
            "title": "Llm Mode",
            "type": "string"
          },
          "service": {
            "title": "Service",
            "type": "string"
          },
          "status": {
            "const": "ok",
            "title": "Status",
            "type": "string"
          },
          "version": {
            "title": "Version",
            "type": "string"
          }
        },
        "required": [
          "status",
          "service",
          "version",
          "api_version",
          "execution_mode",
          "data_mode",
          "llm_mode"
        ],
        "title": "HealthResponse",
        "type": "object"
      }
    }
  },
  "info": {
    "description": "M0 health endpoint only. This API contains no live market data or order submission.",
    "summary": "Local API for QFusion Desktop",
    "title": "QFusion Backend",
    "version": "0.1.0"
  },
  "openapi": "3.1.0",
  "paths": {
    "/api/v1/health": {
      "get": {
        "description": "Return non-sensitive process metadata for the desktop connection check.",
        "operationId": "get_health_api_v1_health_get",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HealthResponse"
                }
              }
            },
            "description": "Successful Response"
          }
        },
        "summary": "Check whether the local M0 backend is responsive",
        "tags": [
          "system"
        ]
      }
    }
  }
}
