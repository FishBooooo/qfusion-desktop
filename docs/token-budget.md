# LLM Token 预算基线

状态：M0 默认 `LLM_MODE=off`，没有运行时 LLM 调用。

后续实现遵循 [ADR-0005](adr/0005-cache-first-llm-budget.md)：

- 内容哈希去重；
- 只发送新增或变化的必要上下文；
- 固定 Schema 和稳定提示词前缀；
- 结构化输出 + Pydantic 验证；
- 有限重试 + 确定性模板降级；
- 每日/月度硬预算；
- Token、费用、缓存命中与模型版本审计。

数值、时间、成本、仓位、回测和风险许可不计入 LLM 任务范围。具体预算数额尚未由用户确认，M0 不虚构默认金额。
