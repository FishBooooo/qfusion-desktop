{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "M0 shell only; no shell, filesystem, network plugin, or order permission.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
